# Base config
spring.application.name=REST & DATA
server.servlet.context-path=/api
server.port=8080

# Connction properties
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/spring
spring.datasource.username=spring
spring.datasource.password=spring

# For logging
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# Always recreates the structure in db
spring.jpa.hibernate.ddl-auto=update

# To pick up data.sql schema.sql
# spring.jpa.defer-datasource-initialization=true
# spring.sql.init.mode=always

# This must be disabled if we want to use data.sql or schema.sql
# spring.jpa.properties.javax.persistence.schema-generation.scripts.action=drop-and-create

# spring.jpa.properties.javax.persistence.schema-generation.scripts.create-target=create.sql
# spring.jpa.properties.javax.persistence.schema-generation.create-source=metadata

# spring.jpa.properties.javax.persistence.schema-generation.scripts.drop-target=drop.sql
# spring.jpa.properties.javax.persistence.schema-generation.drop-source=metadata
